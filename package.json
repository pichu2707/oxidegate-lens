{
  "name": "oxidegate-lens",
  "version": "0.1.0",
  "description": "Read-only presentation layer for OxideGate: shows how many bytes each MCP server costs on every request, plus a Claude Code status line and an experimental OpenCode plugin. It reads the proxy's HTTP endpoints; it never measures anything itself.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "bin": {
    "oxidegate-status": "bin/oxidegate-status.mjs",
    "oxidegate-savings": "bin/oxidegate-savings.mjs"
  },
  "files": [
    "bin",
    "lib",
    "opencode",
    "examples",
    "README.md"
  ]
}
